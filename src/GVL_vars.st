(*
  GVL_vars.st — optional global list
  -----------------------------------------------------------------------
  Core runtime variables are now declared in PLC_PRG Declaration.
  Keep this GVL empty unless you explicitly want shared globals.
*)

VAR_GLOBAL
END_VAR
